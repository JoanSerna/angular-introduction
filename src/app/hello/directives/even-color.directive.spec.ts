import { EvenColorDirective } from './even-color.directive';

describe('EvenColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenColorDirective();
    expect(directive).toBeTruthy();
  });
});
